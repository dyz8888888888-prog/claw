# 2026-06-13

## 可转债日报流水线（项目 in-progress）
- 用户目标：收盘下载可转债数据 + 9:40/10:40/11:40 盘中按涨停概念筛 TOP 10 → 推到微信
- 阶段 1（数据源）已完成：
  - SkillHub `jisilu-cb-daily` 安装到 `~/skills/jisilu-cb-daily/`
  - Cookie 写入 `~/skills/jisilu-cb-daily/references/cookie.json`，试抓 323 条全成功
  - 关键字段覆盖：强赎/下修/溢价率/正股名称/转股价/评级 等 69 列
  - **缺口字段**：剩余规模（亿元）、正股代码 —— 集思录主表没有，需要二次开发或用 mx-data/hithink-cb-selector 补
- 阶段 2（涨停数据）方案已选定：用悟道 MCP `wudao-astock`（已配，HTTP 200，52 个工具）
  - ⚠️ 后续发现 `mcp.json` 实际**没有 wudao-astock 入口**（mcpServers 列表里只有 tdx/qq-mail/... 等 27 个），之前对话里的"5 个悟道工具"其实是 tdx-connector 自己的 limit_up 工具
  - `concept_ranking` / `concept_stocks` / `stock_search` 验证返回路径：`result.structuredContent.data.rows[]`
  - `concept_ranking` 关键字段 `name` / `tsCode` / `limitUpNum` ✅
  - 悟道 MCP **没有"批量正股→概念"工具**（MCP 协议本身的设计：单条查询）
- 阶段 2 改造：用**东方财富 push2** 自己爬全市场 496 概念 × 成分股，构建本地 `data/concept_index.json` 倒排索引
  - 抓取脚本：`scripts/fetch_concept_index.py`（约 5-10 分钟，curl 拉 1000+ 次）
  - 索引格式：`{"600740":[{"tsCode":"BK0438","name":"预制菜"},...], ...}`
  - 复用方式：盘中拉 `concept_ranking` → 本地查表 → 排序
- 阶段 3（推送）已确认：**个人微信**（Server酱/PushPlus），token 待用户填入
- 阶段 4（排序算法）已确认：**A 方案**（一只转债所属多个涨停概念时重复加分）
- 阶段 5（脚本）已写：`scripts/cb_limitup_daily.py`（v3 已重写为 TDX 缓存版）
  - 替换东财 push2 方案 → 改用通达信 MCP `hot_topic_board_family` 构建概念缓存
  - 已通过 `build_concept_cache.py`（i问财版）成功构建 67 只候选正股的 `data/concept_cache.json`（992 个概念，3 批 6.7 秒，无需过滤）
  - 旧版 `build_tdx_cache.py` 已废弃
  - 完整流水线测试通过：59/67 命中涨停概念，TOP1=中汽转债(64分)（i问财概念更多更细，覆盖更广）
  - 微信推送：`PUSH_CONFIG` 待填入 token
  - 【新增】涨停扩散：10:40 对比 09:40，11:40 对比 10:40，概念级别的变化表
  - 快照文件自动保存：`data/score_snapshot_{date}_{time}.json`
  - 09:40 无上期数据时不显示"涨停扩散"列；10:40/11:40 自动显示概念变化 TOP10
  - 文档：`docs/WORKFLOW.md` 已同步更新
- 时间线确认：
  - 集思录每日 **09:00** 更新 CSV（早盘前数据）
  - 09:20 build_tdx_cache.py（预热）
  - 09:40/10:40/11:40 cb_limitup_daily.py（3 次快照，每次只调 1 次 concept_ranking）
  - 共 4 次外网调用/天（预热 1 + 盘中 3）
- 废弃：`fetch_concept_index.py`（东财 push2 方案）

## 沙箱限制（已确认）
- `push2.eastmoney.com` 在当前 sandbox 间歇性封禁（curl exit 56 "schannel: server closed abruptly"）
- Python `requests` / `urllib` 在本机也会触发 RemoteDisconnected（网络栈问题）
- 实测有时通、有时不通，建议**本机外（非 sandbox）跑预热脚本**或等下次通时跑
- `txmcp.tdx.com.cn:3001/txmcp` 401 → 需要 WorkBuddy 主端 OAuth 授权，sandbox 拿不到 token

## 安装/环境总结
- 妙想 Skills（5 个 mx-*）已装到 `~/.workbuddy/connectors/skills/`，MX_APIKEY 永久环境变量
- Iwencai hithink-* Skills（5 个）已装到 `~/skills/`，IWENCAI_BASE_URL/IWENCAI_API_KEY 永久
- 悟道 MCP `wudao-astock` 已写入 `~/.workbuddy/connectors/<userId>/mcp.json` (streamableHttp)
- SkillHub CLI 已知**两个 store** 走同一个二进制（之前误以为是两个，**实际是同一个** `~/.skillhub/skills_store_cli.py`），装 jisilu 时直接复用
