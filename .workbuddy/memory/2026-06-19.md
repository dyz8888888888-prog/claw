# 2026-06-19 工作日志

## cb_monitor 8项Bug修复

### P1 关键修复
- **#1 共识追踪器引用bug** — scheduler.py/server.py 改用 `import core.consensus_tracker as consensus_mod`，全程 `consensus_mod.consensus_tracker` 访问，解决 import 绑定初始 None 的问题
- **#2 全市场概念补全串味** — consensus_tracker.py 改为 `concept_full_prices: dict[str, dict[str, float]]` 按概念分组，逐概念注入各自的全市场数据
- **#3 板块扩散信号泛滥** — signal_engine.py: 删重复 append；加 _BROAD_CONCEPTS 黑名单(融资融券/专精特新等13项)；S级要求概念内>=2个正股联动
- **#4 密钥硬编码** — 3处 API_KEY/webhook 改 `os.environ.get()`；server.py 加 before_request Bearer token 认证；main.py 支持 DASHBOARD_HOST 环境变量；新增 .env.example 和 .gitignore

### P2 优化修复
- **#5 正股买不到判定** — decision_pipeline.py 加 STOCK_SURGING 三档(弱/强但可买/强势拉升/买不到)；区分20cm股票(300/688)单独阈值
- **#6 后验统计PnL全零** — backtest/tracker.py: 买入价用ask1、卖出用bid1；加行情停滞检测(60s无变化→告警)
- **#7 超跌修复归错类** — signal_engine.py: chase→dip；dip_types 加入"超跌修复"

### P3 补漏
- **#8 /api/history路由缺失** — server.py 加 @app.route('/api/history')

## 可转债日报 — 成交单位改为亿元
- tdx_pipeline.py L390: 昨成额显示统一改为亿元（`yest_amount/10000:.2f亿`），不再区分万/亿两档

## cb_monitor P0-P3 全面修复 (15项)
### P0-1 DecisionPipeline接入主循环
- scheduler.py: _run_cycle 增加 _run_decision_pipeline(), 对有信号的债调用4阶段判定, 输出埋伏信号到终端
- pipeline 注入到 Scheduler.__init__ (非全局单例)

### P0-2 amount统一为亿元
- data_fusion.py 新增 fmt_amount() 工具函数 (元→亿元)
- decision_pipeline.py: amount字段改为亿元, 阈值同步调整 (1000万→0.10亿, 2000万→0.20亿)
- signal_engine.py: 两处成交额desc 改用 fmt_amount()
- Snapshot.amount 保持存原始元

### P1-3 后验统计同债去重
- backtest/tracker.py: on_signal 增加 dedup (code+strategy, 120s), 同债同策略不重复追踪

### P1-4 盘后报告脚本
- 新建 scripts/post_market.py: PostMarketReporter 读取 signals_/backtest_ CSV 生成 Markdown 报告
- scheduler.py _handle_non_trading_hours: 15:00-15:30 自动打印盘后报告

### P1-5 主循环耗时监控
- scheduler.py: _run_cycle 记录 t0→cycle_cost, >2.5s WARNING
- shared_state.py: 新增 cycle_cost 字段, 仪表盘可展示

### P2-6 全局变量注入
- scheduler.py: DecisionPipeline 注入到 Scheduler.__init__, 不再使用全局单例
- decision_pipeline.py: 删除底部全局 `pipeline = DecisionPipeline()`

### P2-7 选债池日更
- config.py cov_refresh_interval 已是 86400 (24小时), 无需改动

### P2-8 数据校验
- data_fusion.py: 新增 _validate_snapshots(), 检测单轮价格突跳>5%→丢弃+告警

### P3 细节修复
- TDX健康检查: TdxClient.get() 增加 ping '000001' 验证, 连续3次错误→重建
- akshare按需: 仅缺失>5只时才调用 akshare (原无条件调用)
- 冷却合并: signal_logger.py 去掉 _get_cooldown 方法, 直接内联判断
- 锁优化: shared_state.py 分离 _lock(写) / _read_lock(读)
- 单用户设计: server.py /api/battle 改为每次请求创建局部 DecisionPipeline

## config dataclass + Snapshot独立 + 交易日历
- config.py: 全量改为 dataclass (MonitorConfig + 10个子配置), _ConfigBase mixin 支持 []/.get() 向后兼容
- core/snapshot.py: Snapshot 从 rolling_window.py 迁出, 作为系统核心数据单元独立存在
- data_fusion.py, rolling_window.py: import 路径更新为 core.snapshot
- core/trading_calendar.py: 新建, 用 akshare tool_trade_date_hist_sina 获取官方交易日历, 缓存到 data/trading_calendar.json
- scheduler.py: _is_trading_hours 改为 trading_calendar.is_trading_hours(), 调休日不再误判
- 验证: 今天(20260619 端午节)正确识别为非交易日, 旧代码会误判为交易日

## 全市场概念补全增量优化
- consensus_tracker.py: 新增 `_full_price_cache` (concept→{prices, ts, tickers}, TTL=60s)
- 新增 `_compute_stage_fast()` 轻量版(仅CB池), 用于判断阶段是否变化
- update() 改为增量: 仅刷新 首次/过期/阶段变化 的概念, 其他从缓存读
- 稳定期(概念阶段不变)不再查询TDX全市场数据, 只在概念晋升/退潮时触发

## build_concept_cache 增量→全量
- 用户要求数据全调用: `--incremental` 从 Top50 改为遍历所有概念 (388次API)
- 间隔统一 0.15s, 覆盖全量约 60s
- 保留已有缓存作为基础, 逐概念刷新成分股列表

## 全市场补全参与龙头判定
- consensus_tracker.py: _compute_stage 重构为双数据源(cb_bonds + full_bonds)
- 龙头判定: 全市场+CB池混合排序, 取真正的龙一涨幅做阶段阈值
- 显示统计(dragon_name/followers/limit_up): 仅用CB池, 全市场假数据不参与
- 全市场龙比池内龙强 2%+ → dragons 列表追加 rank=0 标注
- blind_spot 条件从 >15 降为 >0 (有全市场数据就用)

## 问财龙头数据接入共识追踪器
- consensus_tracker.py: 新增 load_leaders(), 自动加载 concept_leaders_*.json
- 构建双向索引: _leader_tickers(concept→{ticker}) + _ticker_leaders(ticker→{concept})
- _compute_stage 新增 leader_match 和 pool_leader_count 字段
- 池内龙的正股在官方龙头列表 → leader_match=true (高置信度)
- scheduler.py: update() 传入 stock_of 映射 (cb_code→stock_code) 供匹配
- 数据为空时 leader_match=None, 不影响原有逻辑

## P0-1: 市场状态识别模块 (全市场数据源)
- 重写: _build_snapshot 改为 akshare 涨停池(全A实际涨停数) + akshare 炸板池 + TDX三大指数(上证/深证/创业板)
- 判定: 指数全涨+涨停≥30→进攻; 指数全跌+涨停<5→冰点; 指数跌+炸板率高→退潮
- 成交量同比: 上证成交额 vs 昨日同期
- scheduler.py: classify() 不依赖CB快照, try/except 容错
- 信号引擎权重联动不变
