# 2026-06-23 工作日志

## 08:44 例行刷新
- Flask API 正常(200)，gen_live_html.py 生成 10773 bytes，ambush=0 sell=0（盘前）
- CloudStudio 部署工具暂不可用；localtunnel (dyz-cb-battle.loca.lt) 在线

## 22:47 数据接口层审查
- 产出 outputs/data_interface_audit.md 全面审查报告
- 覆盖6类数据源: Fuyao / TDX / akshare / pywencai / KPL / 本地缓存
- P0问题: 炸板数数据源割裂(Fuyao涨停+akshare炸板不同源)、Fuyao无重试、TDX健康检查慢
- P1问题: akshare全量池无缓存兜底、概念快照全量刷新过多(388概念每次5批)、API_KEY分散多处
- 整体评分: 数据源多样性5星、接口封装4星、容错3星、调用频率控制3星

## 22:53 P0修复: Fuyao重试 + TDX健康检查提速
- fuyao_client.py: _get() 增加指数退避重试 (max 3次, 1s→2s→4s), HTTP 4xx/5xx/JSON错误不重试
- data_fusion.py: TdxClient 恢复时间优化
  - 空闲超时 30s→15s
  - 连续错误阈值 3→2
  - 新增: 单次错误15s内强制重建, _last_error_time 追踪
  - 抽取 _connect() 方法, 新建后双保险 ping 验证
- 同时确认 Fuyao 不返回炸板数 (15+端点探测均为404)

## 22:55 P1修复: 全量池缓存/概念增量/API_KEY统一/pywencai测试
- bond_selector.py: load_cov_pool() 失败→data/cov_pool_cache.json 兜底; 成功→写缓存 (仅核心字段)
- consensus_tracker.py: ConceptIndexFeed 增量刷新 (仅活跃概念stage≥1, 5分钟全量回归); 删 dead API_KEY+urllib
- scheduler.py: 新增 _get_active_concept_codes() → concept_index.set_active_codes()
- build_concept_cache.py: API_KEY 改用 CONFIG.ext_api, 复用 FuyaoClient._get() 重试
- scripts/test_wencai_fallback.py: 三级降级验证 (pywencai兜底实测: 涨停124+炸板62, 正常)
- 验证: 全部模块加载OK, API_KEY唯一源为CONFIG.ext_api

## cb_monitor 项目启动
- 09:03 启动 main.py（Flask API :5000 + 调度器），TDX 已连接
- 数据源: 通达信行情 + 东方财富，概念映射 1009 只转债

## 自动刷新外链（自动化）
- 09:34/09:41/10:27/11:24/12:21/13:18/14:15/15:13/15:33/16:10/17:07: 每5分钟轮询刷新，Flask API 正常响应，CloudStudio 部署成功
- 最新: 17:07 刷新，ambush=4 sell=0，38656 bytes，外链 https://6c11f531a142445b8b22fe8cbc88ad92.app.codebuddy.work
- 轮询间隔 3秒/次，交易时段 9:30-15:00

## 外链部署
- workbuddy_cloudstudio_deploy 工具不可用（当前环境不支持）
- 改用 npx localtunnel + 固定子域名: https://dyz-cb-battle.loca.lt
- 子域名 dyz-cb-battle 固定，重启后重绑同一子域名即可恢复
- 09:41 修复空白问题: Flask debug=False 模板缓存 → 清 __pycache__ 硬重启 → 数据正确嵌入
- 数据已改为服务端嵌入 window.__EMBEDDED_BATTLE__，不依赖 JS fetch（避免 localtunnel 拦截）

## 10:08 服务恢复（机器重启后）
- Flask + 调度器在机器重启后停止，10:07 手动恢复
- Python venv 重建于 C:\Users\DYZ\.workbuddy\binaries\python\envs\default
- 系统 Python 3.13.12 已含所有依赖（flask/pandas/akshare/mootdx/tabulate），无需额外安装
- localtunnel: 固定子域名 dyz-cb-battle 生效，备用 lucky-wasp-12
- gen_live_html.py: 30091 bytes, ambush=0 sell=0 (盘前)
- bore.exe 位于 ~/.workbuddy/binaries/bore/ 可作备用隧道

## 10:10 外链部署标准化
- 创建 skill: `~/.workbuddy/skills/cb-tunnel/SKILL.md`
- 固化三步流程: (1) 启动Flask main.py (2) gen_live_html.py (3) localtunnel dyz-cb-battle
- 后续自动化或手动生成外链统一走此 skill

## 16:18 概念龙头与情绪数据源调研完成
- 产出报告: `cb_monitor/docs/概念龙头与情绪数据源调研.md`
- 核心结论: kaipanla-crawler（免费 MIT）是最好的免费方案，覆盖涨停原因/概念标签/板块资金流/情绪风向标
- 龙头定义: 不是官宣，是资金投票 → 涨停时间+封单力度+连板高度三维排序
- 情绪六维: 涨停强度/打板质量/接力情绪/亏钱效应/赚钱效应/市场广度
- 落地建议: Phase1 clone kaipanla-crawler + 自建龙头排序 + 东方财富资金流

## 17:14 集成方案设计完成
- 产出方案: `cb_monitor/docs/概念龙头与情绪指标集成方案.md`
- 5阶段分步落地计划，零侵入现有架构
- Phase1: kpl_client 封装 → Phase2: DragonRanker → Phase3: SentimentEngine → Phase4: SectorFlow → Phase5: UI升级
- 6个新文件 + 2个现有文件修改，不修改任何核心逻辑

## 17:34 作战台服务恢复 + 外链刷新
- 依赖已就绪 (venv 10:28安装), 跳过安装步骤
- Flask 服务启动: main.py → localhost:5000, API /api/state 200
- gen_live_html.py: 38656 bytes, ambush=4 sell=0
- localtunnel: dyz-cb-battle 被占用 → 随机分配 kind-kangaroo-25.loca.lt
- 外链: https://kind-kangaroo-25.loca.lt (数据嵌入正常，情绪退潮 advance=18 decline=47)

## 18:05 kaipanla-crawler 克隆+运行+质量评估
- GitHub直连被墙，改用 WebFetch raw → 手工重建 kaipanla_crawler.py + __init__.py
- 依赖已就绪(requests/pandas/urllib3)，无需额外安装
- 13/13 API全部通过，成功率100%，平均延迟1.0s
- 实时数据: 上涨2549/下跌2544, 涨停94, 5连板江钨装备
- 历史数据: 0622涨停149, ST板块28涨停居首, 炸板26只
- 数据质量: 优秀，可集成到 cb_monitor 作战台做情绪辅助
- 已知问题: 异动个股偶尔空JSON; actual_limit_up 计数异常
- 产出: QUALITY_REPORT.md (详细质量分析+集成建议)

## 18:32 kaipanla-crawler 集成到作战台完成
- 新建 3 个核心模块:
  - `cb_monitor/core/kpl_client.py` — KPL数据封装 (日级JSON缓存, 9个接口)
  - `cb_monitor/core/dragon_ranker.py` — 三维龙头排序引擎 (封板时间35%+封单力度35%+连板30%)
  - `cb_monitor/core/sentiment_engine.py` — 六维情绪引擎 (涨停强度/打板质量/接力情绪/亏钱效应/赚钱效应/市场广度)
- 修改 `dashboard/server.py`:
  - 替换旧 `_get_market_sentiment()` (纯涨跌比) → KPL+TDX混合模式
  - 新增 3 个API: /api/dragons, /api/sentiment_detail, /api/sector_flow
  - _build_battle_data 增加 sentiment_detail + dragons 字段
- 修改 `dashboard/templates/index.html`:
  - 新增 4 个tab: 战情|板块|情绪|复盘
  - 新增龙头列表CSS + 六维卡片CSS + 资金流CSS
  - 新增 fetchDragons/renderDragons/fetchSentiment/renderSentiment JS函数
- 测试验证: 0622数据 → 阶段温和, 涨停149, 炸板16.5%, ST东尼龙一88分
- Flask重启后 /api/dragons → 8龙6板块, /api/sentiment_detail → 温和涨停122
- 前端HTML含板块/情绪tab, localhost:5000 外链也生成新版
- 下午完善流程文档 + 评估报告 (40文件/7364行, 架构⭐⭐⭐⭐⭐, 健壮性⭐⭐)
- 集成Fuyao涨停池+连板天梯API: 创建core/fuyao_client.py, 替换market_state.py的akshare/pywencai涨停池+手工晋级率计算, dashboard龙头上线改用Fuyao直取

## 22:34 Fuyao API升级 → 集成涨停池+连板天梯
- 发现Fuyao新增 limit-up-pool (94只涨停,含封板时间/封单/连板/原因) + limit-up-ladder (30日连板矩阵,含seal_nextday)
- 创建 core/fuyao_client.py: 封装get_limit_up_pool/get_limit_up_pool_all/get_limit_up_ladder/get_promotion_rate
- 修改 market_state.py: 涨停池主源 Fuyao → akshare备 → pywencai兜底; 晋级率从手工比对改为连板天梯seal_nextday直算
- 修改 dashboard/server.py: _get_dragons_if_available 优先用Fuyao涨停池直取龙头 (含连板/封单/原因), KPL作为备源
- 测试: 涨停94只, 连板天梯30日, 晋级率26.1%, /api/dragons → ST东尼6连板88分龙一
- Flask硬重启完成, 代码已生效

## 20:56 信号引擎9项改进全部完成
- 复盘分析产出改进方向9条 → 全部实施，修改8个Python文件
- 改进明细:
  1. 移除"正股大跌转债抗跌"毒信号 (胜率35%, 平均-0.17%)
  2. 尾盘洗盘 A→S 升级
  3. 信号置信度评分: 正股大涨(>3%)+转债滞涨(<1%)+主线概念 → B→A
  4. 信号新鲜度: 同转债10分钟内仅保留最强信号
  5. B级信号日上限 20 条
  6. KPL 容错降级: 当日数据不完整时回退最近有效交易日
  7. 统一 signal_id: `{code}_{timestamp_micro}` 格式
  8. /api/signal_accuracy 端点: 基于回测CSV计算各信号类型胜率
  9. 决策管道动态等级调整: 胜率<35%降级, >60%升级 (>20样本)
- 修改文件: config.py, signal_engine.py, signal_accuracy.py(新), kpl_client.py, signal_logger.py, tracker.py, server.py, scheduler.py
- 全部编译通过, /api/signal_accuracy 返回8种信号胜率数据

## 21:55 Flask 硬重启加载新代码
- 先杀旧进程(PID 7572) → 重启 main.py → /api/battle 200
- /api/signal_accuracy → 返回错杀抄底/尾盘洗盘/放量拉升等信号胜率
- /api/dragons → 返回龙头排序 (ST东尼88分第一, 6连板)
- localtunnel dyz-cb-battle.loca.lt 重新上线
- gen_live_html.py 盘后快照: ambush=0 sell=0 (10773 bytes)

## 22:41 接入 Fuyao 概念板块指数到作战台
- fuyao_client.py 新增: get_concept_catalog() / get_concept_snapshots() / _load_concept_name_map()
- consensus_tracker.py 新增: ConceptIndexFeed 类 (388个概念, 每60秒自动节流刷新)
- scheduler.py: 每轮 tick 调 concept_index.refresh()
- server.py 新增端点:
  - /api/concept_sectors → 全量概念涨跌排行 + 市场广度统计
  - /api/concept_stats → 轻量概念市场广度
  - /api/concepts 增强 → 附加 Fuyao 概念指数真实涨跌 (index_pct 字段)
  - _build_battle_data() 增强 → concept_sectors + concept_sector_stats
- 快照接口不含 name, 从 catalog 补名 (修复空名问题)
- 冷启动: API 首次访问自动触发 refresh()
- 盘后验证: 388概念, 涨113/跌252, 领涨 重组蛋白+4.53%, 领跌 金属铅-5.56%
- Flask 已重启 (新代码生效)

## 23:00 项目全面优化审查
- 产出 cb_monitor/outputs/optimization_review.md — 覆盖架构/数据/Web/性能/运维/代码治理 6 维度
- 发现 12 项优化点:
  - P0×3: API Key 硬编码、无日志轮转(936KB/4天)、Flask 无优雅关闭(TDX连接泄露)
  - P1×5: DecisionPipeline 每次请求重复创建、_read_lock 未使用死代码、_detect_lowvol_plunge 死代码(被miskill_buy替代)、概念映射每请求读盘300KB、Dashboard每请求跑完整决策管道
  - P2×4: 模块级可变_prev_prices、fmt_amount重复定义、HTML 518行单文件、30个.pyc未清理
- 建议本周 30min 修 P0-1 + P1-4/5/6，本月 2h 修 P1-7/8 + P0-2

## 23:05 优化项全部修复 (7/7)
- P0-1: API Key 从 config.py 硬编码 → .env (os.environ.get), .env 已在 .gitignore
- P0-2: 日志轮转 — main.py setup_logging() 增加 RotatingFileHandler (10MB/文件×5备份)
- P0-3: Flask 优雅关闭 — server.py start_server() 注册 SIGINT/SIGTERM handler, 释放 TDX
- P1-4: DecisionPipeline 模块级单例 `_get_pipeline()` (复用, 非每请求 new)
- P1-5: 删除 shared_state.py `_read_lock` 死代码 (定义了从未 acquire)
- P1-6: 删除 signal_engine.py `_detect_lowvol_plunge()` 死代码 (32行, 被 miskill_buy 替代)
- P1-7: 概念映射缓存 — 确认全局变量 `_concept_map` 已在内存中缓存 (无需改动)
- P1-8: Dashboard 决策管道缓存 — `_battle_cache` 按 last_update 键复用, 只保留2个 tick
- 顺带修复: /api/history 缺少 @app.route 装饰器 (潜在 404 bug)
- 验证: 全部模块加载 OK, 7/7 通过

## 23:08 重启检测 — 发现并修复 3 项问题
- **问题1 (P0)**: signal.signal(SIGINT/SIGTERM) 在 Dashboard 子线程中注册 → `ValueError: signal only works in main thread` → Flask 启动崩溃
  - 修复: signal 移回 main.py 主线程, server.py 保留 atexit 兜底 + 抽取 `_cleanup_tdx()` 公共函数
- **问题2 (P0)**: python-dotenv 未安装 → `os.environ.get("FUYAO_API_KEY")` 返回空字符串 → Fuyao 所有 API 失败 (概念板块获取失败/概念指数503)
  - 修复: pip install python-dotenv (1.2.2) + config.py 添加 `load_dotenv()` → 388 概念恢复
- **问题3 (P1)**: /api/concept_sectors 和 /api/concept_stats 在数据不可达时返回 HTTP 503 → 改为 200 (数据空≠服务端错误)
- 本地所有 6 端点 HTTP 200: / (28KB), /api/battle (3KB), /api/concept_sectors (61KB), /api/concept_stats (174B), /api/concepts (63B), /api/history (3B)
- 日志轮转生效: monitor.log 9.6KB, RotatingFileHandler 正常
- 综合: Fuyao → 388 概念实时行情, 网络重试 (3次 1s/2s/4s), TDX 15s 快速恢复, .env 安全加载
- localtunnel dyz-cb-battle 子域名被占用, 需用户重新绑定

## 23:25 龙头判定从机械Top8改为共识驱动
- _get_dragons_if_available() 重写: 不再纯机械排序, 改为共识驱动
  - Step1: 从 consensus_tracker 取 stage≥1 的活跃概念及其龙头
  - Step2: 概念热度分 = 涨停数×3 + 跟班数×2 + 龙头涨幅×1 + 池内债数×0.3
  - Step3: Fuyao 涨停池补充连板/封单/涨停时间/原因明细
  - Step4: 动态截断 (冷市≥3只, 热市≤15只, 热度阈值自适应)
  - 共识无数据时回退 KPL
- /api/dragons 端点同步改为共识驱动 + KPL回退
- Flask 重启 + localtunnel dyz-cb-battle 重新上线
- 盘后实测: 共识追踪器无活跃概念(非交易时段) → 回退KPL, 8龙6板块正常

## 23:36 龙头决策参与链路深度分析
- 产出 outputs/dragon_decision_flow.html — 全链路可视化报告
- 核心结论: 龙头不直接参与评分，通过共识阶段间接门控
  1. 龙头角色 = 间接门控，非加权因子
  2. 阶段3(封板)是关键阈值 → stage>=3 → 主线确认 → 策略1(外溢)打开
  3. 策略2(扩散)主线观察即可，但需概念质量过滤(扩散>=30%+RRG非落后)
  4. 信号引擎: 主线内B->A升级(x1.2)是龙头下沉到信号层的入口
  5. Dashboard /api/dragons 排行仅展示，不参与自动化决策
  6. 策略3(错杀修复)完全不依赖龙头
- 影响力排序: 策略1门控(高) > 策略2门控(高) > 概念过滤(中) > 信号升级(中) > Dashboard(无)

## 23:47 例行外链刷新
- Flask API 200, gen_live_html.py → 10824 bytes (ambush=0 sell=0, 盘后)
- CloudStudio: https://f5e7030e8b12420ab708adcee49c628a.app.codebuddy.work
