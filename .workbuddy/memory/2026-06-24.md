# 2026-06-24 工作日志

## cb_monitor 作战台运行

### 06:14 - 首次启动 (live_server.py)
- 启动 live_server.py 端口 8090
- TDX 未连接，回退 demo 模拟数据
- 注意：命令行参数直接传端口号，不支持 --port 前缀

### 10:20 - 情绪数据修复 + 手机外链
- **情绪问题根因**: KPL 缓存的 `ladder_stats` 和 `raw_6d` 为空 `{}`，因为之前爬虫获取连板梯队失败。KPLClient 有内存缓存，修改磁盘文件后只能重启进程生效。
- **修复步骤**:
  1. kaipanla-crawler 直接拉取 2026-06-23 连板梯队数据成功 (一板74/二板14/三板5/高度板1)
  2. 写入缓存 `kpl_cache.json` 的 `ladder_stats`，删除 stale `raw_6d`
  3. 杀掉全部 Python 进程，重启 main.py
- **修复后数据**: 炸板率 33.8%, 连板率 12.8%, 封板成功率 66.2%, 昨日涨停今表现 +1.34%, 昨日连板今表现 +2.88%
- **手机外链**: localtunnel `https://neat-fireant-81.loca.lt` (dyz-cb-battle 子域名被占用)
- **教训**: KPLClient._load_cache() 有内存缓存，磁盘修改需重启进程或调 flush()

### 10:40 - 战情不显示数据修复
- **根因**: MarketStateClassifier 识别为"冰点"→决策管道所有埋伏策略被关闭
- **触发链**:
  1. Fuyao API 盘中涨停计数仅56（盘中部分数据）
  2. Fuyao 晋级率获取失败 → promo=0
  3. `total_limit<60 AND promo<20` → 分类为 freeze（冰点）
  4. DecisionPipeline: freeze 下正股涨停外溢/板块扩散全关闭，仅错杀修复可用（且需 gap>5%）
  5. 最终 ambush_count=0, sell_count=0
- **修复**: `market_state.py` 两处改动
  1. `_fetch_promotion_rate()`: Fuyao 失败 → KPL ladder_stats 连板率备源（12.8%）
  2. `_fetch_limit_up_pools()`: 盘中涨停 < 60 → 自动用 KPL 昨日全量数据兜底（122涨停/28炸板）
- **修复后**: 市场状态 "发酵", ambush_count=3（茂莱转债/集智转债/精装转债）
- **手机外链**: `https://dyz-cb-battle.loca.lt`（固定子域名恢复）

### 10:48 - Fuyao API 测试 + python-dotenv
- **根因**: python-dotenv 未安装 → .env 中的 FUYAO_API_KEY 未加载 → 所有 Fuyao 调用无认证返回空
- **安装**: `pip install python-dotenv`
- **结果**: 涨停池 49只/0.53s, 连板天梯 30天/0.30s, 晋级率 20%/0.30s, 概念目录 388个/0.24s, 概念快照 0.38s
- 重启后市场状态从 KPL 兜底切换到 Fuyao 直接数据

### 10:51 - 情绪指标改为今日实时
- **问题**: 情绪 six indicators (limit_up/blast_ratio/promotion_rate/up_count/down_count) 全来自 KPL 昨日数据
- **根因**: SentimentEngine.merge() 以 KPL 为主, 盘中数据仅补充 pool_* 字段
- **修复**: 新增 `evaluate_intraday_full()` 方法, 用 Fuyao 盘中涨停数+晋级率替换 KPL 昨日数据
  - merge 策略: Fuyao 盘中优先 (limit_up/promotion_rate), KPL 补充历史 (yesterday_*_perf)
- **结果**: 涨停 47(Fuyao实时) | 晋级率 20%(今日) | 情绪 "活跃" | source=fuyao+kpl

### 10:08 - 切换到全量管线 (main.py)
- **问题**: 战情看不到真实数据 → live_server.py 是独立进程，无法访问 main.py 的 shared_state
- **解决**: 停止 live_server.py (8090)，启动 main.py
- main.py 启动内容:
  - BondSelector 加载转债池
  - DataFusion TDX+东方财富数据融合
  - Scheduler 3秒轮询
  - Flask 仪表盘 http://localhost:5000
- 关键发现: live_server.py 和 main.py 是两个独立进程，shared_state 是内存对象不跨进程共享
- 正确用法: 直接运行 main.py（它自带 Flask 仪表盘），live_server.py 仅用于外链隧道部署（需要 cb-tunnel skill）

### 11:05 - /mobile 刷新改为 3 秒
- dashboard/server.py /mobile 路由 `<meta refresh>` 从 5 秒改为 3 秒，与数据轮询周期同步
- localtunnel 子域名 dyz-cb-battle 被占用，分配到 terrible-wasp-50

### 11:10 - /mobile 升级为 4 Tab
- 新增龙头/情绪/复盘三个 Tab（之前只有战情一个页面）
- 龙头: 从共识追踪器直接构建 sector+dragon 数据
- 情绪: 六维指标卡片（涨停/晋级率/封板率/炸板率/涨跌比等）
- 复盘: backtest_tracker 活跃信号 + 浮盈/浮亏统计
- 全部数据在服务端嵌入 HTML，无需 AJAX（localtunnel 兼容）

### 12:17 — 4 Tab 完整数据逻辑审计
- **战情**: DecisionPipeline 4阶段→埋伏/卖出/不做，逻辑正确
- **情绪**: Fuyao盘中 + KPL昨参 → merge分离，今日/昨区分显示，逻辑正确
- **复盘**: backtest_tracker，午休空=预期，逻辑正确
- **龙头**: 发现1个bug — `/api/dragons`(共识路径)返回的dragon对象缺 `cb_pct`/`premium`/`label` 字段，AJAX刷新时 renderDragons 拿不到转债涨跌和同步/滞后标签
- **修复**: active_dragons.append 补全三字段。KPL回退路径不受影响（isKPL检测）
- **13:00开盘后验证**

### 14:28 — 战情/龙头/复盘审计 + 3项修复
- **问题1**: 埋伏concept不一致(胜蓝=新能源汽车) → 新增概念级交集: CB旧概念∩Fuyao板块 → 胜蓝concept→储能
- **问题2**: 春23转债低质量埋伏(stock+2.8%/cb+0.2%) → 板块扩散策略阈值从STOCK_STRONG(>1%)提高到STOCK_SURGING(≥7%)
- **问题3**: 龙头seal_time大量为空 → 概念回退CB无封板数据, 数据显示为空属正常(非stock→CB直接映射)
- 修复效果: 埋伏6→5(春23移除), 胜蓝concept从"新能源汽车"→"储能", 主线=龙头板块一致

### 15:32 — 项目总汇报 + 概念过滤终版
- **概念过滤两层**:
  - _NOISY_CONCEPTS (8个): 深股通/沪股通/融资融券/转融券标的/标普道琼斯A股/MSCI概念/富时罗素概念股/富时罗素概念
  - _BROAD_CONCEPTS (4个): 专精特新/2025年报预增/央企国企改革/国企改革
- **用户确认保留**: 芯片概念、新能源汽车
- **龙排序**: 三维复合(封板时间40%+封单35%+连板25%)
- **龙标签**: 直连(涨停股=CB正股) / 相关(概念回退) / 同步/滞后(溢价判断)
- **实时数据**: 情绪温和, 涨停98, 晋级率25%, 封板率66.2%, 龙头5板块(固态电池29分领军)
