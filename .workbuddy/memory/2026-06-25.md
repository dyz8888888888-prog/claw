# 2026-06-25 工作日志

## 09:23 - gen_live_html.py V2 升级
- 从单页战情升级为完整4Tab自包含HTML (战情/板块/情绪/复盘)
- 嵌入6个API全量数据 (battle/state/dragons/sector_flow/sentiment_detail/backtest)
- 37,894 bytes, dragons=8, sentiment_src=fuyao+kpl

## 09:27 - 切换为实时外链 (localtunnel)
- 用户不需要静态快照, 要实时刷新
- localtunnel dyz-cb-battle.loca.lt 已在线, 直接暴露Flask :5000

## 09:32 - 情绪龙头更改审计 + KPL龙数据补全
- 审计确认: 情绪昨日更改已应用 (fuyao+kpl双源, 6指标实时)
- 龙头Bug: KPL回退路径的dragons缺少cb_pct/premium/label
  - 根因: state.snapshots在API调用的KPL路径中获取为空(竞态)
  - 修复: 改用state.to_dict()获取快照 (与/api/state同路径, 验证可靠)
  - KPL dragons现在通过stock→CB映射 + 快照数据补全溢价/涨跌/标签
- Fuyao HTTP 429 限流 → Fuyao路径间歇失败(回退KPL), KPL补全作为兜底

## 09:42 - 四Tab全部实时刷新
- 根因: 模板只有战情Tab有3s轮询(interval fetchBattle), 其他三Tab仅切换时拉一次
- 修复: 新增__activePage状态, interval按活跃Tab分发 → fetchBattle/fetchDragons/fetchSentiment/fetchReview

## 09:46 - 情绪数据改用Fuyao今日为基调
- 根因: SentimentEngine.merge()以KPL(昨日)为base, Fuyao仅补丁7个字段 → 情绪指标大部分反映昨日
- 修复: merge()方向翻转 — Fuyao可用时以evaluate_intraday_full为基调, KPL仅补充昨日收盘维度(blast_ratio/seal_success_rate/yesterday_*等)
- 效果: limit_up→今日37涨停, promotion_rate→今日25%, phase_classify基于今日数据; 炸板率等保留昨日收盘值(当日无法获取)
- _classify防御: .get()替代直接索引, 防止KeyError

## 09:52 - cached_date修复 + 龙头state变量遮蔽修复
- 情绪cached_date始终显示2026-06-24的根因: api_sentiment_detail 在32行后覆盖 `detail['cached_date'] = kpl._get_recent_trade_date()` → 改为不覆盖(merge已设今日), 兜底fallback
- 龙头0数据的根因: for概念循环中 `for concept, state in all_states` 遮蔽了全局state变量 → KPL路径 `state.to_dict()` 失败 → 改为 `st` 变量名

## 10:22 - 自动化刷新 + 手机外链诊断
- CloudStudio 静态快照: gen_live_html.py 生成 76,650 bytes (53KB嵌入数据), ambush=1 sell=0 dragons=6
- 双外链均在线: CloudStudio (15s刷新) + localtunnel dyz-cb-battle.loca.lt (3s AJAX)
- 静态快照内嵌6个API全量JSON (EMBED变量), renderBattle()在init时调用

## 10:35 - 自动化刷新 #22
- gen_live_html.py → 88,436 bytes, ambush=2 sell=0, dragons=9
- CloudStudio deploy → https://f5e7030e8b12420ab708adcee49c628a.app.codebuddy.work

## 13:05 - 调度器午休唤醒Bug修复
- 根因: 非交易时段 sleep(900) 硬编码, 午休后最多丢失15分钟数据。最后一次重启11:57, 需等到13:12才醒
- 修复: 新增 _seconds_until_next_trading() 精确计算到下次开盘秒数:
  - 午休(11:30-13:00) → 直接算到13:00的秒数, 不再固定900s
  - 盘前(0-9:30) → 算到9:30, 上限900s
  - 盘后 → 900s (避免跨日误差)
- 重启后13:05进入交易: trading=True, snaps=61, ambush=2 sell=0 forbid=8, dragon_sectors=3 dragon_list=9

## 10:51 - 龙头板块渲染崩溃修复
- 根因: renderDragons() 使用KPL字段 `fengdan_score.toFixed(0)` 和 `consecutive_days`，但Fuyao数据没有这些字段 → TypeError崩溃 → catch(e){}静默吞掉 → 显示"暂无数据"
- Fuyao vs KPL 字段差异: Fuyao(board/seal_yi/seal_time/stock_name/link_type/label/cb_pct/premium) vs KPL(consecutive_days/fengdan_score/reason)
- 修复: renderDragons() 兼容双格式 — board || consecutive_days, seal_yi || fengdan_score, label/cb_pct/premium行情展示, stock_name兜底
- 同步修复 gen_live_html.py 和 dashboard/templates/index.html, 重启Flask+localtunnel+CloudStudio

## 10:51 - 龙头板块概念匹配修复
- Fuyao涨停股的泛概念(BROAD_CONCEPTS)改为在Fuyao输入阶段过滤, 不再屏蔽CB池概念
- 效果: 新增"2025年报预增"板块(5涨停), 龙头板块增加到3个

## 10:53 - 概念传导删除
- concept_chain与龙头板块完全同源(Fuyao数据), 属重复展示
- 删除server.py/_build_battle_data()/index.html/gen_live_html.py中所有concept_chain代码
- 影响: /api/battle不再返回concept_chain字段, 战情页空出80px

## 11:50 - MarketStateClassifier与SentimentEngine合并
- 根因: 两套独立系统做同一件事(分类市场阶段), 决策管道只用MarketStateClassifier(硬编码阈值), SentimentEngine只展示
- 合并方案: SentimentEngine成为唯一真相源
  - sentiment_engine.py: 新增 SENTIMENT_TO_MARKET 映射 (overheat→climax, active→ferment, mild→startup, ebb→retreat, freeze→freeze)
  - server.py: _build_battle_data() 情绪先算→驱动决策, 删除 _get_market_sentiment()
  - scheduler.py: SentimentEngine优先, MarketStateClassifier降级为备源
- 数据流: SentimentEngine → phase → state.market_state → DecisionPipeline
- 收益: 情绪与决策统一口径, state.market_state 首次真正有值(之前始终空{})

## 20:11 - 自动化刷新
- Flask API 可达(200), gen_live_html.py → 103,929 bytes, ambush=8 sell=0 dragons=15 sentiment_src=fuyao+kpl
- CloudStudio deploy → https://6c11f531a142445b8b22fe8cbc88ad92.app.codebuddy.work

## 22:40 - 项目打包与固定链接整理
- 用户要求"固定链接每次直接打开项目"
- CloudStudio 永久快照: https://6c11f531a142445b8b22fe8cbc88ad92.app.codebuddy.work (4Tab完整版, 随时可用)
- localtunnel 实时版: https://dyz-cb-battle.loca.lt (需Flask+TDX运行)
- live_server.py 启动信息增加双URL展示
- cb-tunnel skill 更新为双链路说明
